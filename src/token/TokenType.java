package token;

/**
 * The token types.
 */
public enum TokenType {
	LABEL,
	KEYWORD, 
	NUMBER, 
	STRING, 
	IDENTIFIER, 
    ASSIGNMENT, 
    OPERATOR, 
    LEFT_PAREN, 
    RIGHT_PAREN,
    COMMA,
    WHITESPACE
}